services:
  - type: web
    name: convoql-backend
    runtime: python
    pythonVersion: 3.11.0
    buildCommand: pip install --upgrade setuptools wheel && pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT